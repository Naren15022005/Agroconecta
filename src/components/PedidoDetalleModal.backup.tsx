// Backup modal (disabled): simplified stub to avoid build errors while keeping file for reference
"use client";

import React from 'react';

interface Props {
  // Kept minimal for backup
  pedido?: any;
  onClose?: () => void;
  onEstadoChanged?: () => void;
}

export default function PedidoDetalleModalBackup(_props: Props) {
  // This is a disabled/stub version of the original heavy modal.
  // If you need the full implementation, restore from VCS or the original backup.
  return null;
}
